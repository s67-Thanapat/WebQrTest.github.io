# Server-only (set in Vercel Project Settings -> Environment Variables)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

Testing (no UI change)
# Create (also works via /api/issue)
curl -X POST https://<your-vercel-domain>/api/qr/create

# List (also works via /api/stats)
curl "https://<your-vercel-domain>/api/qr/list?limit=10&offset=0"

# Export CSV
curl -L -o qr_generations.csv https://<your-vercel-domain>/api/qr/export-csv

